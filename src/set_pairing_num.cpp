#include "set_pairing_num.h"

void set_pairing_num(int g_menu_Y_selector, boolean g_set_num_remote_too) {
    if (0 <= g_menu_Y_selector <= SET_NUM_MENU_Y_MAX) {
        if (g_set_num_remote_too) {
            ;
           
        } else {
            ;
        }
    }
}